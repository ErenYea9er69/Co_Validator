
export function setCachedResults(query: string, result: any) {
  // Simple no-op for now to keep tavily.ts happy without a real cache implementation
}

export function getCachedResults(query: string) {
  return null;
}
